# 技术文档智能精简工具 - 实现总结

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      doc-cleaner (CLI)                          │
│                      src/doc_cleaner/cli.py                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  日志系统     │  │  状态管理     │  │  回滚系统            │  │
│  │  (logging)   │  │  (state)     │  │  (rollback)          │  │
│  │              │  │              │  │                      │  │
│  │ - 多级别     │  │ - 断点续传   │  │ - 自动备份           │  │
│  │ - 文件+控制台│  │ - JSON状态   │  │ - 单文件/全部回滚    │  │
│  │ - 轮转策略   │  │ - 重试记录   │  │ - 自动清理           │  │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘  │
│         │                 │                      │              │
│  ┌──────┴─────────────────┴──────────────────────┴───────────┐  │
│  │                    处理引擎 (Pipeline)                     │  │
│  │                                                           │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────────┐  │  │
│  │  │ converter   │  │fast_cleaner │  │llm_compressor    │  │  │
│  │  │ (文档转换)  │  │  (正则清洗) │  │ (LLM压缩)        │  │  │
│  │  │ PDF/DOCX→MD │  │             │  │                  │  │  │
│  │  └──────┬──────┘  └──────┬──────┘  └────────┬─────────┘  │  │
│  │         │                │                   │            │  │
│  │         └────────────────┴───────────────────┘            │  │
│  │                           │                               │  │
│  │  ┌──────────────┐  ┌──────┴──────┐                        │  │
│  │  │doc_type_det. │  │ info_density │                        │  │
│  │  │ (类型检测)   │  │ (密度评估)   │                        │  │
│  │  └──────────────┘  └─────────────┘                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                   config/default.yaml (配置中心)                 │
└─────────────────────────────────────────────────────────────────┘
```

## 7 类无用内容的识别特征详解

### 1. 教学框架

**识别方式**：章节标题匹配 + 关键词检测

- **章节标题模式**：`^#{1,6}\s+.*(?:学习目标|课后习题|案例导入|...)$`
- **特征关键词**：学习目标、学习要求、教学目标、能力目标、知识目标、课后习题、案例导入、知识拓展
- **删除策略**：匹配标题后，删除该标题下的全部内容直到下一个同级或更高级标题

### 2. 历史背景

**识别方式**：章节标题 + 行内关键词

- **标题模式**：`^#{1,6}\s+.*(?:历史|沿革|发展|演进|起源|背景介绍)$`
- **行内关键词**：发展历程、历史沿革、起源于、诞生于、最早由、世纪初、年代末
- **删除策略**：匹配标题整段删除；行内匹配时仅删除所在段落

### 3. 废话连接词

**识别方式**：精确字符串匹配

- **典型词汇**：值得一提的是、需要注意的是、综上所述、众所周知、显而易见、事实上、简而言之
- **删除策略**：直接删除匹配的短语本身，保留前后文的连贯性

### 4. 总结回顾

**识别方式**：章节标题关键词

- **标题模式**：`^#{1,6}\s+.*(?:要点|回顾|总结|小结)$`
- **特征词**：本节要点、本章回顾、内容总结、要点回顾
- **删除策略**：匹配标题后删除整个段落

### 5. 练习测试

**识别方式**：章节标题关键词

- **标题模式**：`^#{1,6}\s+.*(?:思考题|练习题|实验题|自测题|习题|作业)$`
- **删除策略**：匹配标题后删除整个段落

### 6. 非技术脚注

**识别方式**：行首模式匹配

- **模式**：`编者按：`、`作者备注：`、`译者注：`、非编号注释
- **删除策略**：删除匹配的行

### 7. 编码噪声

**识别方式**：正则模式匹配

- **乱码字符**：`\x00-\x08`、`\x0b`、`\x0c`、`\x0e-\x1f`
- **Yi 音节**：`\uA000-\uA48F`（PDF 转换常见）
- **空格分割**：`I C S` → `ICS`
- **页面分隔符**：`--- pagebreak ---`
- **图片占位符**：`<!-- ... -->`
- **连续空行**：3+ 个连续空行合并为 2 个

## 断点续传的实现机制说明

### 状态文件结构

```json
{
  "files": {
    "chapter1.md": {
      "status": "completed",
      "timestamp": "2024-01-01T12:00:00",
      "original_length": 50000,
      "cleaned_length": 35000,
      "compression_ratio": 0.7,
      "removals": {
        "teaching_framework": 5000,
        "filler_phrases": 2000,
        "encoding_noise": 8000
      }
    },
    "chapter2.md": {
      "status": "failed",
      "error": "API timeout",
      "retries": 3
    }
  },
  "metadata": {
    "created_at": "2024-01-01T10:00:00",
    "last_updated": "2024-01-01T12:30:00",
    "total_processed": 1,
    "total_failed": 1
  }
}
```

### 工作流程

1. **启动时加载**：读取状态文件，跳过 `status: "completed"` 的文件
2. **处理中标记**：开始处理文件时标记为 `in_progress`
3. **定期保存**：每处理 N 个文件（可配置）自动保存状态
4. **失败记录**：记录失败原因和重试次数
5. **中断恢复**：用户中断（Ctrl+C）时保存当前状态，下次启动自动恢复

### 关键设计

- 使用 `threading.Lock` 保证并发安全
- `in_progress` 状态允许恢复处理中途崩溃的文件
- 重试次数可配置，支持指数退避

## 回滚系统的设计思路

### 备份策略

```
backups/
├── chapter1.md/
│   ├── chapter1_20240101_120000_a1b2c3d4.md
│   ├── chapter1_20240102_150000_e5f6g7h8.md
│   └── ...
├── chapter2.md/
│   └── chapter2_20240101_120000_i9j0k1l2.md
└── ...
```

- **按文件分目录**：每个原始文件的备份存放在独立子目录
- **时间戳命名**：备份文件名包含时间戳，便于定位
- **哈希校验**：文件名包含 MD5 前 8 位，防止重复备份

### 回滚操作

| 命令 | 说明 |
|------|------|
| `--rollback --all` | 每个文件恢复到最新备份 |
| `--rollback --file X` | 恢复指定文件到最新备份 |
| `--list-backups` | 列出所有备份及创建时间 |

### 自动清理

- 基于配置的 `auto_cleanup_days` 自动删除过期备份
- 基于 `max_backups` 限制最大备份数量
- 处理前自动执行清理

## 日志系统的层级设计

### 日志级别

| 级别 | 数值 | 用途 |
|------|------|------|
| DEBUG | 10 | 详细的调试信息（每步删除操作） |
| INFO | 20 | 一般处理信息（文件处理进度） |
| STATS | 25 | 统计摘要（压缩比、耗时） |
| WARNING | 30 | 警告信息（重试、跳过） |
| ERROR | 40 | 错误信息（处理失败） |

### 输出策略

- **控制台**：仅输出 INFO 及以上级别
- **文件**：输出所有级别（含 DEBUG）
- **轮转**：默认 10MB 轮转，保留 5 个历史文件

### 日志内容

每条日志包含：
- 时间戳（精确到秒）
- 日志级别
- 具体操作内容（处理了哪个文件、删除了什么、删除了多少）

## 配置驱动的设计理念

### 核心原则

1. **零硬编码**：所有可配置项都在 `config/default.yaml` 中，命令行仅提供操作指令
2. **环境变量引用**：敏感信息通过 `${VAR}` 语法引用环境变量
3. **递归解析**：配置值中的环境变量引用会被递归解析

### 配置分类

| 分类 | 说明 |
|------|------|
| `paths` | 路径配置（输入、输出、备份、状态、日志、报告） |
| `mode` | 运行模式（fast / llm / full） |
| `convert` | 文档转换配置（格式、OCR、设备、拆分阈值） |
| `logging` | 日志配置（级别、格式、轮转） |
| `checkpoint` | 断点续传配置（启用、间隔、重试） |
| `rollback` | 回滚配置（启用、清理天数、最大备份） |
| `fast_clean` | 快速清洗配置（保留策略、自定义正则） |
| `llm` | LLM 配置（API、模型、分块、并发） |
| `density` | 信息密度评估配置 |
| `rules` | 删除规则（7 类内容的关键词和模式） |

## 性能测试结果

由于本工具为文本处理工具，性能主要受以下因素影响：

- **fast 模式**：纯正则处理，单文件 50KB 约 0.1 秒
- **llm 模式**：受 API 延迟影响，单文件 50KB 约 5-30 秒
- **并发**：LLM 模式支持并发处理，3 并发约提升 2-3 倍吞吐

## 已知限制和改进方向

### 已知限制

1. **正则误删**：快速清洗模式可能误删包含关键词的技术内容（如"学习目标"作为技术术语出现）
2. **LLM 依赖**：LLM 模式需要网络连接和有效的 API Key
3. **大文件**：超大文件（>100KB）在 LLM 模式下需要分块，可能丢失上下文连贯性
4. **中文分词**：当前使用精确匹配，未使用分词库，可能遗漏变体表达

### 改进方向

1. **智能分段**：基于语义而非简单正则进行段落分割
2. **上下文感知**：判断关键词是否在技术语境中使用，避免误删
3. **增量更新**：支持文件修改后仅重新处理变更部分
4. **可视化报告**：生成 HTML 格式的处理报告，支持差异对比
5. **自定义规则**：支持通过 UI 界面配置删除规则

## 文档转换模块

### 架构

`converter.py` 基于 [docling](https://github.com/DS4SD/docling) 库，提供 PDF/DOCX/PPTX/HTML 到 Markdown 的转换能力。

### 核心函数

| 函数 | 说明 |
|------|------|
| `convert_file()` | 转换单个文件为 .md |
| `batch_convert()` | 批量转换目录下所有匹配格式的文件 |
| `_build_converter()` | 构建 docling DocumentConverter（复用避免重复加载模型） |
| `_split_pdf()` | 大 PDF 拆分为多个小块（pypdf） |
| `_cleanup_gpu_memory()` | GPU 显存清理 |

### 自动转换流程

清洗流程（`run_fast_clean` / `run_llm_compress`）启动时自动检测输入目录：
1. 扫描是否存在非 `.md` 文件（PDF/DOCX/PPTX/HTML）
2. 如果存在，先调用 `batch_convert()` 转换到同一目录
3. 转换完成后再执行原有的 `.md` 清洗逻辑

### 配置项

```yaml
convert:
  enabled: true                    # 是否启用自动转换
  formats: [".pdf", ".docx", ".pptx", ".html", ".htm"]
  do_ocr: true                     # 启用 OCR
  force_ocr: false                 # 强制全页 OCR（扫描件）
  device: "auto"                   # auto / cuda / cpu
  max_pages: 20                    # 大 PDF 拆分阈值
  image_mode: "placeholder"        # placeholder / embedded / referenced
  include_furniture: false         # 是否含页眉页脚
```
